# Rive runtime deployment checkpoint

This file intentionally marks the `test/rive-runtime-proof` branch for the automatic TEST Pages deployment pipeline.

The Rive runtime proof is isolated behind `?rive=1`; the normal SuperBook Experience remains unchanged until the real character runtime passes the visual gates.
